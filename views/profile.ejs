<!doctype html>
<html>
<head>
    <title>Scrambled</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
    <link rel="stylesheet" href="style.css">
</head>
<body>
<div class="container">

    <div class="page-header text-center jumbotron">
        <h1><span class="fa fa-anchor"></span> Profile Page</h1>
        <a href="/logout" class="btn btn-default btn-sm">Logout</a>
        <!-- button connected to get -->
        <a href="/playPage" class="btn btn-default btn-sm">Play</a>
    </div>

    <div class="row">

        <!-- LOCAL INFORMATION -->
        <div class="col-sm-6">
            <div class="well">
                <h3><span class="fa fa-user"></span> Local</h3>

                <% if (user.local.email) { %>
                    <p>
                        <strong>id</strong>: <%= user._id %><br>
                        <strong>email</strong>: <%= user.local.email %><br>
                        <strong>password</strong>: <%= user.local.password %>
                    </p>

                    <a href="/unlink/local" class="btn btn-default">Unlink</a>
                <% } else { %>
                    <a href="/connect/local" class="btn btn-default">Connect Local</a>
                <% } %>

            </div>
        </div>
        <div class="col-sm-6">
          <h3>Your Words</h3>
          <ul class="messages">
          <% for(var i=0; i<results.length; i++) {%>
            <li class="message">
              <span>Scrambled: <%= results[i].sWord %> |</span>
              <!-- completed word -->
              <span>Unscrambled: <%= results[i].cWord %> |</span>
              <!-- Input word -->
              <span>Your Guess: <%= results[i].iWord %></span>
              <% if(results[i].cWord == results[i].iWord){ %>
              <span><i class="fa fa-thumbs-up" aria-hidden="true"></i></span>
              <% }else{ %>
              <span><i class="fa fa-thumbs-down" aria-hidden="true"></i></span>
              <% } %>
              <span><i class="fa fa-trash-o" aria-hidden="true"></i></span>
            </li>
          <% } %>
          </ul>

          

          

</div>

<script src="main.js"></script>

</body>
</html>
